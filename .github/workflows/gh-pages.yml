name: Deploy GitHub Pages

on:
  push:
    branches:
      - canary
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: Install
        run: sudo npm i -g npm@latest &&  npm install

      - name: Build
        run: npm run docs:build && cp CNAME dist

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          BRANCH: gh-pages
          FOLDER: dist
